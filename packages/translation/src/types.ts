export interface ITranslations {
  example: string;
  the_example: string;
  not_found_error_message: string;
  not_found_error_solution: string;
  status_must_be_active_inactive_deleted: string;
  x_is_required: string;
  name: string;
  subscription: string;
  plan: string;
  the_plan: string;
  account: string;
  the_account: string;
  benefits: string;
  value: string;
  version: string;
  plan_version: string;
  expires_at: string;
  kind: string;
  user: string;
  the_user: string;
  already_exists_error_message: string;
  already_exists_error_solution: string;
  not_accepted_file_type_error_message: string;
  file_not_found: string;
}
